# Portfolios — Guía de Extracción

## Conceptos Creativos Replicables

### Tier "WOW" (alto impacto, alta dificultad)
1. **Mundo 3D navegable** (Bruno Simon) → R3F + physics engine
2. **macOS simulator** (Renovamen) → Window manager + app framework
3. **CRT retro terminal** (edhinrichsen) → Three.js + terminal emulator
4. **Displacement sphere** (HamishMW) → Three.js ShaderMaterial

### Tier "Profesional" (balance impacto/esfuerzo)  
1. **Spotlight cursor** (Brittany Chiang) → CSS radial-gradient + onMouseMove
2. **Blur-fade text** (Magic UI Portfolio) → Motion + intersection observer
3. **3D voxel mascot** (Craftzdog) → R3F + GLB model + orbit controls
4. **Motion Primitives layout** (Nim) → TextEffect + MorphingDialog

### Tier "Quick Win" (fácil de implementar)
1. **Tilt.js cards** (Simplefolio) → vanilla-tilt.js, 3 líneas
2. **Staggered nav reveal** (Brittany Chiang) → CSS animation-delay
3. **Section fade-in** (casi todos) → IntersectionObserver + opacity transition
4. **Gradient themes** (Simplefolio) → CSS custom properties swap

## Componentes Extraíbles Prioritarios
- `SpotlightCursor` de Brittany Chiang
- `MacOSWindow` + `MacOSDock` de Renovamen
- `DisplacementSphere` de HamishMW
- `BlurFade` de dillionverma
